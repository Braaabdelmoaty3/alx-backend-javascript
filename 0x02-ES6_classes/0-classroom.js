/* eslint-disable */
export default class ClassRoom{
    constructor(maxStudentsSize){
        this._maxStudentsSize = maxStudentsSize;
    }
}








/*class User {
    constructor(id, username, salary) {
        this.id = id;
        this.username = username;
        this.salary = salary;
        this.msg = function(){
            return `${this.username} this is your salary ${this.salary}`
        }
    }
}

let ahemd = new User(100, "ahmed", 2000) ;
console.log(ahemd.msg());*/
